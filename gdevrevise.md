Look at gihub issue #$ARGUMENTS to see the original ask, as well as a development plan for the request. There may be multiple revisions of the dev plan so read the whole thread and make sure you understand the LATEST dev plan that was approved.

Look for feedback either on the main issue thread or in the PR that is attached. Look also for code comments and reviews. If there is feedback, revise the changes based on the feedback and update the PR by pushing up the changes to the remote branch.

Update the PR description as well if necessary.

If the PR has been accepted and merged, then delete the local branch and remove the "dev-acked" label and add the "dev-complete" label to the issue. 